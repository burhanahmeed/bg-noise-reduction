{
  "name": "bg-noise-reduction",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "cd web && npm run build",
    "install:web": "cd web && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}